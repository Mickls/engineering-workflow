interface:
  display_name: "Issue Slicing"
  short_description: "把大需求拆成可独立完成、可验证的垂直切片"
  default_prompt: "按 issue slicing 流程把这个需求或计划拆成可独立完成和验证的垂直切片，标注依赖和覆盖约束，并准备回写到 .codex/engineering-workflow/issues 下的 plan 或 roadmap。"
